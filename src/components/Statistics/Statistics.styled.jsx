import styled from "styled-components";

const StaticticsList = styled.ul`
  margin-top: 30px;
`
const StaticticsItem = styled.li`
  font-size: 28px;
  line-height: 1.7;
`

export { StaticticsList, StaticticsItem}